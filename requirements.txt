# Web UI + API wrapper around the cis-bench CLI.
cis-bench==0.5.2
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
# Direct WorkBench form login (username/password option). Also a cis-bench dep.
requests>=2.31.0
# Policy generator (SABIC .docx templating): lxml preserves the template's
# namespaces/styles when rewriting the document body.
lxml==5.3.0
# Optional AI drafting of the policy narrative via the Claude API. The app runs
# fine without it (static templates); enable by setting ANTHROPIC_API_KEY.
anthropic==0.120.0
